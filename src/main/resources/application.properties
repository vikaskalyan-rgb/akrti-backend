

# ?? Database ??????????????????????????????????????????????
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-dry-cake-a1hwodlu-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&prepareThreshold=0&tcpKeepAlive=true}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_hRfLngEFv45t}
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? HikariCP ??????????????????????????????????????????????
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=300000
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.datasource.hikari.data-source-properties.tcpKeepAlive=true
spring.datasource.hikari.data-source-properties.socketTimeout=30
spring.datasource.hikari.data-source-properties.connectTimeout=10

spring.main.lazy-initialization=true
spring.datasource.hikari.initializationFailTimeout=0
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.maximum-pool-size=2
spring.jpa.open-in-view=false
server.port=${PORT:8080}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# ?? JWT ???????????????????????????????????????????????????????
jwt.secret=${JWT_SECRET:akriti-adeshwar-super-secret-key-2025-minimum-256-bits-long}
jwt.expiration.ms=2592000000

# ?? Twilio (kept but unused) ??????????????????????????????????
twilio.account.sid=${TWILIO_ACCOUNT_SID:your_account_sid}
twilio.auth.token=${TWILIO_AUTH_TOKEN:your_auth_token}
twilio.whatsapp.from=${TWILIO_WHATSAPP_FROM:whatsapp:+14155238886}

# ?? App config ????????????????????????????????????????????????
app.admin.username=admin
app.admin.password=admin123
app.admin.name=Ramesh Kumar
app.admin.phone=9840000000
app.monthly.maintenance=4200
app.cors.allowed.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
app.otp.simulate=false

# ?? Scheduling ????????????????????????????????????????????????
spring.task.scheduling.enabled=true

# ?? Fast2SMS ??????????????????????????????????????????????????
fast2sms.api.key=${FAST2SMS_API_KEY:}

brevo.api.key=${BREVO_API_KEY:}